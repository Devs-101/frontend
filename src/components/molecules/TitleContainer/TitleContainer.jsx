import React from 'react'
import { TitleContainerStyle } from './TitleContainer.styles'

export function TitleContainer({ children }) {
  return (<TitleContainerStyle>{children}</TitleContainerStyle>)
}
