import React from 'react'
import { ModalFormContainerStyle } from './ModalFormContainer.styles'

export function ModalFormContainer({ children }) {
  return <ModalFormContainerStyle>{children}</ModalFormContainerStyle>
}
