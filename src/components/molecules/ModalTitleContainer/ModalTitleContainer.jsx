import React from 'react'
import { ModalTitleContainerStyle } from './ModalTitleContainer.styles'

export function ModalTitleContainer({ children }) {
  return <ModalTitleContainerStyle>{children}</ModalTitleContainerStyle>
}
